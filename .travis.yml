language: cpp

compiler:
    - clang
    - gcc

matrix:
  include:
    - os: linux
      dist: bionic
    - os: osx
      osx_image: xcode10.2

script:
  - mkdir build
  - cd build
  - cmake -DRUN_TESTS=ON -DCMAKE_VERBOSE_MAKEFILE=ON ..
  - cmake --build .
  - ctest --output-on-failure
